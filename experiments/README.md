# Experiments

Use this folder for lightweight experiment notes and reproducible command logs.
Large outputs, checkpoints, cached heatmaps, and cached features should go under `outputs/`,
which is ignored by Git.